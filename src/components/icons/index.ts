// 导出图标组件
export { Icon } from './Icon'

// 导出类型定义
export type { IconName, IconSize, IconColor, IconProps, IconConfig } from '@/types/icon'

// 导出配置
export { getIconSize, getIconColorClass } from '@/config/icon'
